let count=0;
do{    //execute this block at least once
    console.log("Count value " + count ); //print count variable value on console // + is  concatination ,count is a variable
    count++; //increase count variable value by 1
}while(count<5); //evalute expression 
//when count variable value became 5 while expression return false because 5 is not less than 5;