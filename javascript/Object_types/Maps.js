//create map
const roles=new Map();//to call object specify parantheses

//using map we can assign particular role to the user.using set method we  can specify key and value pair to the map object
roles.set(t1,user1);
roles.set(t1,user2);
console.log(roles);